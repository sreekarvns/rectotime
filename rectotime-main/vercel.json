{
  "ignoreCommand": "git diff --quiet HEAD^ HEAD ./ml ./requirements.txt ./requirements.legacy.txt",
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "framework": "vite",
  "env": {
    "SKIP_ENV_VALIDATION": "true"
  }
}
